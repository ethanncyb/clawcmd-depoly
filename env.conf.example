# ClawCMD - Initial Infrastructure Setup Configuration
# Main deployment script for the club's main infrastructure
# 
# Purpose: First-time setup for new infrastructure or after Proxmox reset
# This configuration sets up basic remote access with NetBird and Cloudflare Tunnel
# 
# Copy this file to env.conf and modify the values

# Container Specifications
CT_ID=1000
CT_HOSTNAME=netbirdlxc
CT_CPU=2
CT_RAM=1024
CT_SWAP=1024
CT_STORAGE=8

# Network Configuration
CT_BRIDGE=vmbr0
CT_NETWORK=dhcp
CT_UNPRIVILEGED=1

# NetBird Configuration
NETBIRD_ENABLED=1
NETBIRD_MANAGEMENT_URL=
NETBIRD_SETUP_KEY=

# Cloudflare Tunnel Configuration
CLOUDFLARED_ENABLED=1
CLOUDFLARED_TOKEN=

# Template Configuration
# CT_OS and CT_VERSION are used for template auto-detection if CT_TEMPLATE is empty
CT_OS=debian
CT_VERSION=13
# CT_TEMPLATE: Full template path (e.g., local:vztmpl/debian-13-standard_13.1-2_amd64.tar.zst)
# Leave empty for auto-detection (will use CT_OS and CT_VERSION to find latest template)
CT_TEMPLATE=

# Storage Configuration
# These are TWO DIFFERENT storage pools:
# 
# TEMPLATE_STORAGE: Storage pool where container templates are stored/downloaded
#   Used for: Template auto-detection and template downloads
#   Default: local
#   Example: local, local-lvm, or any storage that supports templates
TEMPLATE_STORAGE=local
#
# STORAGE_POOL: Storage pool where the container's disk will be saved
#   Used for: Container root filesystem (where the container data is stored)
#   Default: local-lvm
#   Example: local-lvm, local, or any storage that supports containers
STORAGE_POOL=local-lvm

# Optional: Tags for Proxmox (will be auto-generated based on services enabled)
# Default: core-services
# Will automatically add: netbird (if enabled), cloudflared (if enabled)
CT_TAGS=core-services

# Proxmox Host Tools Installation
# Set to 1 to install tmux, iftop, htop on Proxmox host (default: 1)
INSTALL_PROXMOX_TOOLS=1

